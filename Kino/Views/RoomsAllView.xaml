<local:AllViewBase x:Class="Kino.Views.RoomsAllView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kino.Views.Abstract"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary Source="MainWindowResources.xaml"/>
    </UserControl.Resources>

    <DataGrid SelectedItem="{Binding Path=SelectedRoom, Mode=TwoWay}"
              AutoGenerateColumns="False" ItemsSource="{Binding List}" IsReadOnly="True" FontSize="12">
        <DataGrid.Columns>
            <DataGridTextColumn Header="ID sali" Binding="{Binding IdSali}" Width="75"/>
            <DataGridTextColumn Header="Nazwa" Binding="{Binding Nazwa}" Width="200*"/>
            <DataGridTextColumn Header="Numer" Binding="{Binding Numer}" Width="100*"/>
            <DataGridTextColumn Header="Liczba miejsc" Binding="{Binding LiczbaMiejsc}" Width="100*"/>
            <DataGridTextColumn Header="Sala 3D" Binding="{Binding Sala3D}" Width="100*"/>
        </DataGrid.Columns>
    </DataGrid>

</local:AllViewBase>
