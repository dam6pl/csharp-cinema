<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkieTowayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <!-- Bindujemy kolumny z bazy danych -->
                <DataGridTextColumn Header="IdTowaru" Binding="{Binding IdTowaru}"/>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Nazwa}"/>
                <DataGridTextColumn Header="Kod" Binding="{Binding Kod}"/>
                <DataGridTextColumn Header="Stawka VAT Sprzedaży" Binding="{Binding StawkaVatTowaruSprzedaz}"/>
                <DataGridTextColumn Header="Stawka VAT Zakupu" Binding="{Binding StawkaVatTowaruZakup}"/>
                <DataGridTextColumn Header="Cena" Binding="{Binding Cena}"/>
                <DataGridTextColumn Header="Marża" Binding="{Binding Marza}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
